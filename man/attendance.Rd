% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attendance.R
\docType{data}
\name{attendance}
\alias{attendance}
\title{NYC School Attendance Sample}
\format{
A tibble with 46,333 rows and 12 variables:
\describe{
  \item{school_dbn}{Character. New York City school DBN (district-borough-number) identifier.}
  \item{date}{Date. Observation date.}
  \item{school_year}{Factor. School year, with levels such as `20182019`, `20192020`, and `20202021`.}
  \item{enrolled}{Integer. Number of students enrolled on that date.}
  \item{present}{Integer. Number of students recorded as present on that date.}
  \item{absent}{Integer. Number of students recorded as absent on that date.}
  \item{released}{Integer. Number of students recorded as released on that date.}
  \item{dow}{Factor. Day of week derived from `date`.}
  \item{month}{Factor. Month derived from `date`.}
  \item{district}{Character. Two-character district code extracted from `school_dbn`.}
  \item{borough_code}{Character. One-letter borough code extracted from `school_dbn`.}
  \item{borough}{Character. Borough name derived from `borough_code`.}
}
}
\source{
Derived from the New York City Open Data dataset
\emph{2018--2021 Daily Attendance by School}. The packaged object is a cleaned,
sampled subset rather than the full source file.
}
\usage{
attendance
}
\description{
`attendance` contains daily school-level attendance counts and
derived calendar and location variables for a stratified sample of New York
City schools.
}
\details{
A cleaned sample of daily attendance records for 100 New York City public
schools drawn from the 2018--2019, 2019--2020, and 2020--2021 school years.
The sample was created by excluding District 75 schools, sampling schools at
the school level with proportional stratification by borough, and then
retaining all daily observations for the sampled schools.


The package dataset is derived from the public file
\emph{2018--2021 Daily Attendance by School}. Cleaning and sampling were
performed as follows:
\itemize{
  \item Variable names were cleaned to snake case.
  \item Dates were parsed differently across school years because the raw file
  stores 2020--2021 dates in a different format from the earlier years.
  \item `dow` and `month` were created from `date` as unordered factors.
  \item `school_year` was converted to a factor.
  \item Incomplete cases were removed.
  \item `district`, `borough_code`, and `borough` were derived from `school_dbn`.
  \item District 75 schools were excluded.
  \item A sample of 100 schools was drawn proportionally by borough, and all
  daily records for those schools were retained.
}
}
\examples{
head(nyc_attendance_sample)

}
\keyword{datasets}
